def setup():
    size(750,750)
def draw():
    if keyPressed:
        if key=='a':
            background(0)
    if keyPressed:
        if key=='d':
            img = loadImage("123.jpg")
            #image(img,230,100,500/2,500/2)
            textSize(25)
            text(u"Паук Гладиатор",260,320)
            text(u"Для человека-безопасен",225,390)
    
